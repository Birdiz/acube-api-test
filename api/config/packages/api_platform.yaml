api_platform:
    title: File Conversion API
    description: 'Upload a file, request a conversion, poll the status URL for the result.'
    version: 1.0.0
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']
